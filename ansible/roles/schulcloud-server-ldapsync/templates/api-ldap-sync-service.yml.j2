apiVersion: v1
kind: Service
metadata:
  name: api-ldapsync-svc
  namespace: {{ NAMESPACE }}
  labels:
    app: api-ldapsync
  annotations:
    prometheus.io/scrape: 'true'
    prometheus.io/port: '3030'
spec:
  type: ClusterIP
  ports:
    - port: {{ PORT_SERVER }}
      targetPort: 3030
      protocol: TCP
      name: api-ldapsync
  selector:
    app: api-ldapsync
