apiVersion: onepassword.com/v1
kind: OnePasswordItem
metadata:
  name: notification-secret
  namespace: {{ NAMESPACE }}
  labels:
    app: notification
spec:
  itemPath: "vaults/{{ VAULT }}/items/server"