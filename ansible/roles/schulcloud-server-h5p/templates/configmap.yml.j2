apiVersion: v1
kind: ConfigMap
metadata:
  name: api-h5p-configmap
  namespace: {{ NAMESPACE }}
  labels:
    app: api-h5p
data:
  EXIT_ON_ERROR: "true"
  NEST_LOG_LEVEL: "{{ NEST_LOG_LEVEL }}"
  JWT_PUBLIC_KEY: "{{ JWT_PUBLIC_KEY }}"
  SC_DOMAIN: "{{ DOMAIN }}"
