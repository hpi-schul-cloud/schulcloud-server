apiVersion: v1
kind: Service
metadata:
  name: tldraw-svc
  namespace: {{ NAMESPACE }}
  labels:
    app: tldraw
spec:
  type: ClusterIP
  ports:
    - port: 3345
      targetPort: 3345
      protocol: TCP
      name: tldraw
  selector:
    app: tldraw
