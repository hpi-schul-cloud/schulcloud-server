apiVersion: v1
kind: Service
metadata:
  name: admin-api-serv-svc
  namespace: {{ NAMESPACE }}
  labels:
    app: admin-api-serv
spec:
  type: ClusterIP
  ports:
    # port for http managing drawing data
    - port: 4030
      targetPort: 4030
      protocol: TCP
      name: admin-api-serv
  selector:
    app: admin-api-serv