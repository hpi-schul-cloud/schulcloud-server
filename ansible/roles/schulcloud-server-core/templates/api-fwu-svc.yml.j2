apiVersion: v1
kind: Service
metadata:
  name: api-fwu-svc
  namespace: {{ NAMESPACE }}
  labels:
    app: api-fwu
spec:
  type: ClusterIP
  ports:
    - port: {{ PORT_FWU_LEARNING_CONTENTS }}
      targetPort: 4446
      protocol: TCP
      name: api-fwu
  selector:
    app: api-fwu
