apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: api-admin-svc-monitor
  namespace: {{ NAMESPACE }}
  labels:
    app: api-admin
spec:
  selector:
    matchLabels:
      app.kubernetes.io/name: api-admin-svc
  endpoints:
    - path: /metrics
      port: api-metrics
