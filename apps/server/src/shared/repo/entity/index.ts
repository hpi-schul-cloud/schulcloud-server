/**
 * Entities declare how Objects look like which can be identified by an Id.
 * Entities must be classes and the repository must provide instances of these classes (not just plain objects).
 */

export * from './base.entity';
