# Review 11.09.2023

- nested modules should be handled like other modules, meaning they should be protected from accessing each other like other modules. In particular, each (internal) module should define an index file, to define what can be imported into other modules (see how much effort it would be)
