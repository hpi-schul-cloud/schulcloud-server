export enum HelpdeskProblemState {
	OPEN = 'open',
	CLOSED = 'closed',
	SUBMITTED = 'submitted',
}

export enum HelpdeskProblemType {
	CONTACT_ADMIN = 'contactAdmin',
	CONTACT_HPI = 'contactHPI',
}

export enum SupportType {
	PROBLEM = 'problem',
	WISH = 'wish',
}
