```typescript
interface BoardNodeBuilder {
  buildColumnBoardNode(do: Card): BoardNode[];
  buildCardNode(): BoardNode[];
}

interface BoardNodeBuildable {
  useBoardNodeBuilder(builder: BoardNodeBuilder): BoardNode[]
}

class ColumnBoard implements BoardNodeBuildable {

  useBoardNodeBuilder(builder: BoardNodeBuilder): BoardNode[] {
    builder.buildColumnBoardNode(this);
  }

}

class BoardNodeBuilderImpl implements BoardNodeBuilder {
  buildColumnBoardNode(do: Card): BoardNode[] {
    // do stuff with the specific columnBoard instance
  }
}

const nodes = new BoardNodeBuilderImpl().buildColumnBoardNode(columnBoard);
```