const { ScopeService } = require('./ScopeService');
const { ScopeListService } = require('./ScopeListService');
const { ScopePermissionService } = require('./ScopePermissionService');
const { ScopeMembersService } = require('./ScopeMembersService');

module.exports = {
	ScopeService,
	ScopePermissionService,
	ScopeListService,
	ScopeMembersService,
};
