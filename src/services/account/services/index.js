const { supportJWTServiceSetup } = require('./SupportJWTService');
const { jwtTimerServiceSetup } = require('./jwtTimerService');

module.exports = {
	supportJWTServiceSetup, // TODO: remove me
	jwtTimerServiceSetup,
};
